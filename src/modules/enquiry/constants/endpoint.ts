// Endpoint constants for the enquiry module
export const ENQUIRY = {
  BASE: '/enquiry/enquiry',
  byId: (id: number) => `/enquiry/enquiry/${id}`, // Endpoint to get enquiry by ID
  // Add more endpoints as needed
};
